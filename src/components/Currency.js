import React from "react";
import { Text, View} from 'react-native';
import styled from "styled-components/native";

const Currency = props => {
    return (
        <View>
            <Text>Currency component</Text>
        </View>
    );
};

export default Currency;